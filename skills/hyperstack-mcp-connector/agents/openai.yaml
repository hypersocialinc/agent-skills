interface:
  display_name: "Hyperstack MCP Connector"
  short_description: "Expose Convex functions as an OAuth (Clerk) remote MCP connector for Claude/ChatGPT, hosted in Convex"
  default_prompt: "Use $hyperstack-mcp-connector to expose Convex functions as an OAuth-secured remote MCP connector that Claude and ChatGPT can call as the signed-in user — hosted inside Convex via convex-mcp-gateway, with Clerk as the authorization server."
